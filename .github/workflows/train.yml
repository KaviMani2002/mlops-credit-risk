name: Train Multi Model with DVC

on:
  workflow_dispatch:
  push:
    paths:
      - 'src/**.py'
      - '.github/workflows/train.yml'

jobs:
  train-model:
    runs-on: self-hosted

    env:
      MLFLOW_TRACKING_URI: http://20.106.177.129:5000
      AZURE_STORAGE_ACCOUNT: ${{ secrets.AZURE_STORAGE_ACCOUNT }}
      AZURE_STORAGE_KEY: ${{ secrets.AZURE_STORAGE_KEY }}
      GITHUB_ACTIONS: true

    steps:
      - name: 📥 Checkout repo
        uses: actions/checkout@v3

      # ---------- PYTHON VENV ----------
      - name: 🐍 Setup Python virtual environment
        run: |
          python3 -m venv .venv
          source .venv/bin/activate
          python3 -m pip install --upgrade pip
          pip3 install --upgrade "cryptography<41"
          pip3 install "dvc[azure]==2.45.2" --no-cache-dir
          pip3 install -r requirements.txt --no-cache-dir

      # ---------- DVC CONFIG ----------
      - name: ⚙️ Configure DVC Azure Remote
        run: |
          source .venv/bin/activate
          dvc remote add -d azureremote azure://dvcdata || true
          dvc remote modify azureremote connection_string "${{ secrets.AZURE_DVC_CONNECTION }}"

      - name: ⬇️ Pull data from Azure DVC remote
        run: |
          source .venv/bin/activate
          export LD_LIBRARY_PATH=/usr/lib/x86_64-linux-gnu:$LD_LIBRARY_PATH
          dvc pull

      # ---------- DATA INGESTION ----------
      - name: 📂 Run data ingestion
        run: |
          source .venv/bin/activate
          python3 src/data_ingestion.py

      # ---------- DATA CLEANING ----------
      - name: 🧹 Run data cleaning
        run: |
          source .venv/bin/activate
          python3 src/data_cleaning.py

      # ---------- FEATURE ENGINEERING ----------
      - name: 🏗️ Run feature engineering
        run: |
          source .venv/bin/activate
          python3 src/feature_engineering.py

      # ---------- MODEL TRAINING ----------
      - name: 🚀 Train models and log to MLflow
        run: |
          source .venv/bin/activate
          python3 src/train_multi_model.py

      - name: 🚀 Promote Models to Staging stage
        run: |
          source .venv/bin/activate
          python3 src/promote_best_models.py

      # ---------- CLEANUP ----------
      - name: 🧹 Cleanup runner workspace and caches
        if: always()
        run: |
          echo "Cleaning up runner workspace and caches..."
          sudo rm -rf "$RUNNER_WORKSPACE"/*
          rm -rf ~/.cache/pip
          rm -rf ~/.dvc/cache
          sudo rm -rf /home/azureuser/actions-runner/_work/*
          echo "Cleanup complete."
