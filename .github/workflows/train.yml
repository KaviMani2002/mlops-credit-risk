name: Train Multi Model with DVC

on:
  workflow_dispatch:
  push:
    paths:
      - 'src/**.py'
      - '.github/workflows/train.yml'

jobs:
  train-model:
    runs-on: self-hosted

    env:
      MLFLOW_TRACKING_URI: http://20.106.177.129:5000
      AZURE_STORAGE_ACCOUNT: ${{ secrets.AZURE_STORAGE_ACCOUNT }}
      AZURE_STORAGE_KEY: ${{ secrets.AZURE_STORAGE_KEY }}
      GITHUB_ACTIONS: true

    steps:
      - name: 📥 Checkout repo
        uses: actions/checkout@v3

      - name: 🐍 Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: 📦 Install dependencies safely
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install "dvc[azure]>=3.0.0"   # latest DVC for md5/.dir support
          pip install azure-storage-blob

      - name: 🔍 Verify DVC version
        run: dvc --version

      - name: ⚙️ Configure DVC Azure Remote
        run: |
          dvc remote add -d azureremote azure://dvcdata
          dvc remote modify azureremote connection_string "${{ secrets.AZURE_DVC_CONNECTION }}"

      - name: ⬇️ Pull data from Azure DVC remote
        run: dvc pull data/raw/accepted_2007_to_2018Q4.csv

      - name: 🔍 SonarQube Scan
        run: |
          docker run --rm \
          -e SONAR_HOST_URL="http://20.106.177.129:9000" \
          -e SONAR_TOKEN="${{ secrets.SONAR_TOKEN }}" \
          -v "${{ github.workspace }}:/usr/src" \
          sonarsource/sonar-scanner-cli \
          -Dsonar.projectKey=credit-risk \
          -Dsonar.sources=/usr/src


      # ---------- DATA INGESTION ----------
      - name: 📂 Run data ingestion
        run: python src/data_ingestion.py

      # ---------- DATA CLEANING ----------
      - name: 🧹 Run data cleaning
        run: python src/data_cleaning.py

      # ---------- FEATURE ENGINEERING ----------
      - name: 🏗️ Run feature engineering
        run: python src/feature_engineering.py

      

      # ---------- MODEL TRAINING ----------
      - name: 🚀 Train models and log to MLflow
        run: python src/train_multi_model.py

      - name: 🚀 Promote Models to Staging stage
        run: python src/promote_best_models.py

      # ---------- CLEANUP ----------
      - name: 🧹 Cleanup working directory
        run: |
          echo "Cleaning up /home/azureuser/actions-runner/_work ..."
          rm -rf /home/azureuser/actions-runner/_work/*
          echo "Cleanup completed."

